[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python manage.py migrate --noinput && python manage.py collectstatic --noinput && python manage.py loaddata fixtures/places_curated.json --ignorenonexistent && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
