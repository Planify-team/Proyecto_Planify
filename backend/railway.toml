[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'python manage.py migrate --noinput; python manage.py ensure_superuser || true; exec gunicorn config.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 1 --timeout 120'"
healthcheckPath = "/health/"
healthcheckTimeout = 30
